<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <select id="list" resultType="com.sky.vo.SetmealVO">
        select s.*,c.name as category_name from setmeal as s left join category as c on s.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                and s.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and s.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and s.status = #{status}
            </if>
        </where>
        order by s.update_time desc
    </select>
    <select id="getById" resultType="com.sky.vo.SetmealVO">
        select s.*,c.name as category_name from setmeal as s left join category as c on s.category_id = c.id
        where s.id = #{id}

    </select>
    <delete id="deleteBatch" parameterType="java.util.List">
        delete from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="update" parameterType="com.sky.vo.SetmealVO">
        update setmeal set name = #{name}, category_id = #{categoryId}, status = #{status}, update_time = #{updateTime}
        where id = #{id}
    </update>
</mapper>